# Nexus API Configuration
DATABASE_PATH=./data/nexus.db
JWT_SECRET=change-me-to-a-random-secret
PORT=3001

# Production notes:
# PORT=3010 (production, matches systemd service)
# JWT_SECRET should be a random 32+ char string
# Uploads stored in ./data/uploads/{tenantId}/
# CORS origins configured in packages/api/src/index.ts
