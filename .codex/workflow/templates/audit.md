# Audit Record Template

Body reference only. Create durable records with the project wrapper from `.codex/workflow/profile.json` `paths.workflowWrapper` (Nexus: `node .codex/scripts/nexus-workflow.mjs record-audit ...`) so required frontmatter is generated and gates can validate the record.

Scope:

Verdict: pass | fail | partial | blocked

Auditor:

Audit Areas:

Findings:

- none

Command Run IDs:

Embedded Command Evidence:

Artifacts Or Record Evidence:

Residual Risk:

Next Action:
