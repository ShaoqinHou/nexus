# Patch Record Template

Body reference only. Create durable records with the project wrapper from `.codex/workflow/profile.json` `paths.workflowWrapper` (Nexus: `node .codex/scripts/nexus-workflow.mjs record-patch ...`) so required frontmatter, worktree/branch hashes, and work-slice links are generated.

Summary:

Scope:

- worktree
- branch

Files:

- path/to/file

Reason:

Work Slices:

Routing:

Agent Or Worker:

Validation Expected:

Review Required:

Follow-Up:
