---
schema: nexus-routing/v1
id: ROUTING-YYYYMMDDTHHMMSSZ-short-summary
created: YYYY-MM-DDTHH:MM:SSZ
author: codex
---

# Routing Decision

Summary:

Task type:

Selected route:

Worker:

Note: delegated patch records must include both `--worker` and `--routing`.

Rejected routes:

Assigned write scope:

Required verification:

Fallback trigger:

Fallback target:

Review requirement:

Evidence:
