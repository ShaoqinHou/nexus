# Workflow Capabilities

Required capabilities run immediately. Optional deployment, design Zoo/Gym, dependency audit, CI, and hooks can remain disabled stubs until project content exists. The portable fixture is copied by `workflow:portability-check` and then current system scripts are injected.
