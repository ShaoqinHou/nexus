# Workflow System Layer

Reusable system kernel, engine, hook dispatcher, and portable empty-project fixture.
Copy system code, rewrite project policy/profile/adapter sources, then test against
the target policy.
